[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dc34-image"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "Pillow>=10.0,<11.0",
    "pyserial>=3.5",
]

[project.scripts]
dc34-image = "dc34_image.send_image:main"